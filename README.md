# vexspace.github.io

vexspace 공개 문서 사이트(GitHub Pages). **이 저장소는 산출물이다** — 원본은 비공개 저장소 `vexspace/calit`의 `site/`
디렉터리와 `docs/privacy-policy-publish.md`이며, 그쪽 워크플로가 이 저장소의 `main`을 통째로 덮어쓴다.
여기서 직접 고친 내용은 다음 동기화 때 사라진다.
